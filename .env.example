# ---------------------------------------------------------------------------
# 拾光 Gleam —— 环境变量示例
# ---------------------------------------------------------------------------
#
# 本项目【不需要】任何 API Key、Token 或账号。
# 它是一个完全离线的本地工具：不调用 AI API、不调用云端服务、
# 不上报遥测、不做远程配置。断网后功能完整可用。
#
# 这个文件之所以存在，只是为了明确告诉你：这里没有任何密钥需要填写。
# 你可以复制成 .env，但即使 .env 为空，应用也能正常工作。
#
# 下面是唯一支持的两个可选调试开关（都与网络无关）：

# 启动时把扫描器的详细日志打印到终端（仅开发调试用）。
# 取值：1 开启，留空或 0 关闭。
GLEAM_DEBUG=

# 覆盖默认扫描目录（用分号 ; 分隔多个目录）。
# 一般不需要设置 —— 在应用的「设置」页面里添加目录更方便。
# 示例：GLEAM_EXTRA_DIRS=D:\我的备份\codex;E:\projects
GLEAM_EXTRA_DIRS=
